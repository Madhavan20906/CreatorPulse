# CreatorPulse — 2:45 Hackathon Demo Script & Judge Walkthrough

> **Target Duration**: 2 minutes 45 seconds  
> **Audience**: Hackathon Judges & Technical Reviewers  
> **Key Value Proposition**: The first autonomous YouTube creator operating system with a closed compounding feedback loop (Signal → Explainable Math → Content Factory → 7-Rule QA → Measurement → Memory).

---

## Quick Reference: 3-Minute Golden Path

| Phase | Screen | Time | Core Technical Action / Story |
| :--- | :--- | :--- | :--- |
| **01** | Landing / Pulse | `0:00 - 0:25` | Introduce the problem: creators drown in disconnected AI generators. CreatorPulse is an autonomous operating system that compounds from every publish. |
| **02** | Channel Intelligence | `0:25 - 0:45` | Real baseline performance across 42 catalog videos. Topic clustering and historical fit. |
| **03** | Opportunity Map | `0:45 - 1:15` | **Traceable Math**: Expand "Why this score?" breakdown (`Score = 0.35×Fit + 0.30×Hist + 0.20×Nov - 0.15×Collision`). Show counterfactual collision test. |
| **04** | Content Factory & QA | `1:15 - 1:50` | Select Voice Profile ("Thoughtful Technical"), generate full coherent package (hook, script, chapters, shorts, SEO), and run the **7-rule Deterministic QA Gate**. |
| **05** | Schedule & Publish | `1:50 - 2:05` | Approve package with 1-click calendar synchronization. |
| **06** | Analytics & Feedback | `2:05 - 2:30` | **The Climax: Closed Learning Loop**. Select "Cycle 1 (v3 → v4)" or "Cycle 2 (v4 → v5)". Record actual views vs prediction. Watch memory upgrade live and opportunities re-rank. |
| **07** | Memory & Audit Trace | `2:30 - 2:45` | Inspect updated persistent knowledge graph and open the real-time agent execution log (`/activity`). |

---

## Detailed Step-by-Step Script with On-Screen Actions

### `[0:00 - 0:25]` Act 1: The Problem & The Operating System
* **On Screen**: Landing Page (`/`) then transition to Dashboard (`/dashboard`).
* **Voiceover**:
  > *"Every AI creator tool on the market today is an open-loop generator: you type a prompt, get generic text, copy-paste it into YouTube, and the AI learns nothing. If a video flops or goes viral, the model is just as blind next Monday.*
  >
  > *Meet CreatorPulse — the first closed-loop operating system for serious creators. Instead of guessing, CreatorPulse turns channel performance into defensible strategy, carries it from idea to publish, and compounds intelligence with every single video."*
* **Visual Action**:
  - Show the **Judge Golden Path Stepper** at the top of `/dashboard`.
  - Highlight the **Creator Workflow Economy Card**: **8 hours 15 minutes saved** per video (a 97% reduction from 8.5 hours to 15 minutes).

---

### `[0:25 - 0:45]` Act 2: Channel Intelligence & Semantic Collision
* **On Screen**: Navigate to Channel Intelligence (`/channel`), then Before I Publish (`/before-publish`).
* **Voiceover**:
  > *"CreatorPulse ingests our channel's 42 catalog videos and calculates a statistical baseline of 43,000 views. But here's what sets our backend apart:*
  >
  > *When we test a new idea — like 'Why productive creators build slower systems' — we don't use basic token matching. CreatorPulse runs Google Gemini dense embeddings with vector cosine similarity across our entire catalog to detect cannibalization risks before we spend a single hour shooting."*
* **Visual Action**:
  - Click into `/before-publish` and hit **"Evaluate this idea"**.
  - Show the 72% collision detection with existing catalog titles and the suggested pivot.

---

### `[0:45 - 1:15]` Act 3: Explainable Opportunity Map (No Black Box)
* **On Screen**: Navigate to Opportunity Map (`/opportunities`).
* **Voiceover**:
  > *"In the Opportunity Map, every recommendation is defensible. Section 50 of our architecture requires complete mathematical transparency.*
  >
  > *Judges can click 'Why this score?' directly on the opportunity card to inspect the exact attribution equation: 35% Audience Fit + 30% Historical Fit + 20% Novelty minus 15% Cannibalization Risk.*
  >
  > *Notice our #1 ranked video: 'Why AI agents work in a demo but fail in production' with a score of 91/100 and a 1.96x predicted baseline lift."*
* **Visual Action**:
  - Click **"Why this score? ▼"** on the top opportunity card to expand the formula breakdown in real-time.
  - Click the card to open `OpportunityDetail` (`/opportunities/opp-ai-agents-prod`).

---

### `[1:15 - 1:50]` Act 4: Multi-Surface Content Factory & Deterministic QA
* **On Screen**: `OpportunityDetail` → Content Detail (`/content/...`) → Quality Gate (`/qa`).
* **Voiceover**:
  > *"Inside the opportunity, we select our voice profile — 'Thoughtful Technical' — and generate the complete creative package.*
  >
  > *In seconds, CreatorPulse synthesizes a long-form script with chapters, three derived short-form candidates with timestamps, native social posts, SEO title variants, and thumbnail composition.*
  >
  > *Before anything ships, we pass it through our Deterministic Quality Gate: seven hard, code-level validators checking hook length, retention anchors, collision thresholds, and audience fit. No LLM hallucinations rubber-stamping bad content."*
* **Visual Action**:
  - Switch through tabs: **Long-form**, **Shorts**, **Social**, **SEO**, **Thumbnail**.
  - Click **"Export package"** (downloads clean Markdown).
  - Open `/qa` and click **"Run Quality Gate"** (shows 7/7 passed verification checks).
  - Click **"Approve & schedule"** to lock it onto the Calendar (`/calendar`).

---

### `[1:50 - 2:30]` Act 5: The Climax — Multi-Cycle Compounding Memory
* **On Screen**: Analytics (`/analytics`).
* **Voiceover**:
  > *"Now for the defining breakthrough of CreatorPulse: closing the feedback loop.*
  >
  > *After publishing, our video achieves 84,200 views — 1.96x our baseline. Let's record the measurement.*
  >
  > *Watch what happens when I click Record:*
  > *1. Memory upgrades from version 3 to version 4.*
  > *2. AI Agent production reliability is reinforced as our channel's highest-converting pillar.*
  > *3. And most critically: the Opportunity Map immediately re-ranks, boosting our second agent idea by +5 points to 96/100.*
  >
  > *We can even simulate Cycle 2 with video-41 to demonstrate that memory compounds continuously from v4 to v5."*
* **Visual Action**:
  - Click preset **"Cycle 1 (v3 → v4)"** and hit **"Record & Upgrade Memory"**.
  - Highlight the green **"Hero Closed Feedback Loop"** confirmation card with the live state shift diff.
  - Click **"Inspect re-ranked opportunities"** to show the dynamic +5 pt lift live!

---

### `[2:30 - 2:45]` Act 6: Persistent Knowledge & Agent Audit Trail
* **On Screen**: Memory (`/memory`) and Agent Activity (`/activity`).
* **Voiceover**:
  > *"Under Memory, our four-layer knowledge graph stores topic, format, hook, and timing intelligence that never degrades.*
  >
  > *And in our Agent Activity log, every single strategic call, vector cosine check, QA verification, and memory shift is transparently auditable.*
  >
  > *CreatorPulse turns content creation from a roulette wheel into a compounding flywheel. Thank you."*
* **Visual Action**:
  - Show the 4 memory panels (`v4/v5`).
  - Open `/activity` showing the real-time agent audit traces.
  - End on the CreatorPulse logo with all systems nominal.
